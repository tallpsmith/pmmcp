{
  "mcpServers": {
    "pmmcp": {
      "command": "podman",
      "args": ["run", "-i", "--rm", "--name", "pmmcp", "pmmcp", "--pmproxy-url", "http://host.containers.internal:44322"]
    }
  }
}

// NOTE: The pmmcp image is not yet published to a registry (see GitHub issue #1).
// Build it locally first:
//   podman build -t pmmcp .
//
// Replace --pmproxy-url with your pmproxy host. If using the bundled docker-compose
// test harness, use http://host.containers.internal:44322
